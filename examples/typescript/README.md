# TypeScript / Node

```bash
npx tsx consulta.ts 01001000
MYCEP_API_KEY=cep_live_... npx tsx consulta.ts 01001000
```

Node 20+ (usa `fetch` e `AbortSignal.timeout` nativos). Para tipos completos e os demais
endpoints, use o [SDK](../../sdk/typescript).
